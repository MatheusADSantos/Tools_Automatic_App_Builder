git_url("https://github.com/MatheusADSantos/Certificates_Match")

storage_mode("git")

git_branch("appName")

app_identifier("bundle")

username("appleID(email)")

# type("development") # The default type, can be: appstore, adhoc, enterprise or development
